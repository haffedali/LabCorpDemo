import {makeStyles} from '@material-ui/styles';

export const useStyles = makeStyles((theme) => ({
    container: {
        width: "80vw",
        height: '85vh',
        padding: '0px'  
    },
    displayContainer: {
        overflow: 'hidden'
    },
    gridContainer: {

    },
    buttonContainer: {
        padding: '0.5vw',
        paddingTop: '1.5vw'
    }
  }));